package com.plantmanager.entity.enums;

/**
 * Health status of a plant.
 * Used to track plant condition.
 */
public enum HealthStatus {
    /** Plant is healthy and thriving */
    GOOD,
    /** Plant needs watering */
    THIRSTY,
    /** Plant has health issues (pests, disease, etc.) */
    SICK
}
