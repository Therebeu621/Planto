apiVersion: v1
kind: Service
metadata:
  name: {{ include "plant-backend.fullname" . }}
  labels:
    {{- include "plant-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "plant-backend.selectorLabels" . | nindent 4 }}
